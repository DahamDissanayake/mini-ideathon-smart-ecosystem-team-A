#ifndef ENV_H
#define ENV_H

// Name the node advertises. The dashboard finds it by its Nordic UART service
// UUID rather than by name, so this is free to be anything readable.
#define BLE_DEVICE_NAME "test letGo device"

#endif  // ENV_H
